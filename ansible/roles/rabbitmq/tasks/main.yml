- name: Run or install RabbitMQ
  delegate_to: localhost
  connection: local
  containers.podman.podman_container:
    name: tinystage-{{ container_name }}
    hostname: "{{ container_name }}.{{ domain }}"
    image: docker.io/library/rabbitmq:4-management
    state: started

